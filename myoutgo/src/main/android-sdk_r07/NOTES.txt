== [1] INSTALL ECLIPSE ==

sudo apt-get install eclipse (and its dependences)
"eclipse rcp"


== [2] DOWNLOAD ANDROID SDK (Software Development Kit) ==

from http://developer.android.com/sdk/index.html

	=== ADD PATH ===

	export PATH=${PATH}:/home/coto/android-sdk-linux_86/tools


== [3] INSTALLING ADT (Android Development Tools - Plugin to Eclipse) ==

   1. Start Eclipse, then select Help > Install New Software.
   2. In the Available Software dialog, click Add....
   3. In the Add Site dialog that appears, enter a name for the remote site (for example, "Android Plugin") in the "Name" field.

      In the "Location" field, enter this URL:

      https://dl-ssl.google.com/android/eclipse/

      Note: If you have trouble acquiring the plugin, you can try using "http" in the URL, instead of "https" (https is preferred for security reasons).

      Click OK.
   4. Back in the Available Software view, you should now see "Developer Tools" added to the list. Select the checkbox next to Developer Tools, which will automatically select the nested tools Android DDMS and Android Development Tools. Click Next.
   5. In the resulting Install Details dialog, the Android DDMS and Android Development Tools features are listed. Click Next to read and accept the license agreement and install any dependencies, then click Finish.
   6. Restart Eclipse.

	== Configuring the ADT Plugin ==

	Once you've successfully downnloaded ADT as described above, the next step is to modify your ADT preferences in Eclipse to point to the Android SDK directory:

	   1. Select Window > Preferences... to open the Preferences panel (Mac OS X: Eclipse > Preferences).
	   2. Select Android from the left panel.
	   3. For the SDK Location in the main panel, click Browse... and locate your downloaded SDK directory.
	   4. Click Apply, then OK

== [4] Adding (or Updating) Android Platforms and Other Components (SDK Components) ==

	1.  Run on terminal$ android (This is one of two options, Windows dosen't matter)
	2.  It will open "the Android SDK and AVD Manager"
	3.  To download components, use the graphical UI of the Android SDK and AVD Manager, to browse the SDK repository, select new or updated components for download, and then install the selected components in your SDK environment. 

==========================================

== Create an Android Virtual Devices (AVDs) 

select a target
	android list targets
create an AVD
	android create avd -n my_android1.5 -t 2
List AVDs created
	android list avds
	


